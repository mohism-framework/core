import { Parser } from '../src/engine/service/http/paramParser/index';
import { IncomingMessage } from 'http';


describe('paramParse', () => {
  it('parseBody', () => {
    // const req:IncomingMessage = {};
    // const p = Parser(req);
  });
});